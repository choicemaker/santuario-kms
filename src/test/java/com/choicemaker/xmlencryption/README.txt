These tests require default AWS credentials provider with permission to use
the default master key specified below. See "Providing AWS Credentials in the
AWS SDK for Java" (http://links.rph.cx/24kqE58)
